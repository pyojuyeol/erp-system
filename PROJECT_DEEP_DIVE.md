# ERP System — 프로젝트 완전 정복 가이드

이 문서는 프로젝트의 모든 설계 결정과 그 이유, 실제 겪은 문제와 해결 과정을 정리한 것입니다. 면접이나 코드 리뷰에서 "왜 그렇게 했나요?"라는 질문에 답할 수 있도록 구성했습니다.

---

## 1. 프로젝트를 한 문장으로 설명한다면

"React와 NestJS로 만든 사내 ERP 시스템으로, 인사·부서·급여·재고·회계·근태 6개 업무 도메인을 JWT 인증과 역할 기반 권한 관리로 보호하며, Nginx와 PM2로 배포까지 완료한 풀스택 프로젝트입니다."

---

## 2. 왜 이 기술 스택을 선택했는가

### 왜 React + TypeScript인가
- 컴포넌트 재사용성과 생태계(Ant Design, TanStack Query 등)가 풍부해 ERP처럼 폼/테이블이 많은 화면을 빠르게 구축 가능
- TypeScript로 API 응답 타입을 명시해, 백엔드 DTO 변경 시 프론트에서 컴파일 타임에 오류를 잡을 수 있음

### 왜 NestJS인가 (Express 대신)
- 프론트가 TypeScript이므로 백엔드도 TypeScript로 통일하면 컨텍스트 스위칭 비용이 줄어듦
- Spring Boot와 유사한 모듈-컨트롤러-서비스-DI(의존성 주입) 구조라, 대규모 프로젝트에서 계층 분리가 명확함
- Guard, Interceptor, Pipe 같은 공식 확장 포인트가 있어 인증/인가/검증 로직을 선언적으로 분리하기 쉬움

### 왜 Prisma인가
- 스키마 파일 하나로 마이그레이션과 타입까지 자동 생성되어 생산성이 높음
- 실제로 Prisma 7으로 넘어오면서 `datasource url`을 스키마에 직접 못 쓰게 바뀐 걸 겪었고, `prisma.config.ts` + Driver Adapter(`@prisma/adapter-pg`) 방식으로 마이그레이션하며 최신 버전 대응 경험을 쌓음

### 왜 PostgreSQL인가
- 관계형 데이터(직원-부서-급여-근태의 복잡한 FK 관계)를 다루기에 적합
- 트랜잭션, Unique 제약, 외래키 제약을 활용해 데이터 정합성을 DB 레벨에서도 보장

### 왜 Redis를 넣었는가
- 세션/캐시 확장을 염두에 두고 구성 (현재는 기본 세팅만 되어 있고, 대시보드 통계 캐싱 등으로 확장 가능한 여지를 남겨둠)

### Docker 도입 시점과 순서에 대한 의도
- 처음부터 Docker로 시작하지 않고, **먼저 Nginx/PM2/PostgreSQL/Redis를 직접 설치하고 연결하는 과정을 학습**한 뒤, 이후 단계에서 Docker Compose로 전환하는 순서를 택했습니다.
- 이렇게 한 이유는 컨테이너가 감춰주는 것(리버스 프록시 설정, 프로세스 관리, 서비스 간 네트워킹)을 먼저 손으로 겪어봐야, Docker Compose가 실제로 무엇을 대신해주고 있는지 이해할 수 있다고 판단했기 때문입니다.
- 최종적으로는 [8장](#8-인프라-심화--docker-cicd-부하테스트)에서 다루는 것처럼 Docker Compose + GitHub Actions CI/CD까지 구축했습니다.

---

## 3. 인증 구조 — 가장 깊게 파고들 만한 부분

### Access Token과 Refresh Token을 나눈 이유
Access Token 하나만 쓰면 두 가지 딜레마가 생깁니다.
- 만료 시간을 길게 잡으면(예: 7일) → 토큰이 탈취됐을 때 공격자가 오래 사용 가능
- 만료 시간을 짧게 잡으면(예: 15분) → 사용자가 자주 로그아웃되어 불편

그래서 **"자주 죽는 짧은 토큰(Access) + 이걸 갱신해주는 오래 사는 토큰(Refresh)"** 구조로 둘 다 해결합니다.

### Refresh Token을 왜 쿠키에 저장했는가 (localStorage 대신)
- localStorage는 JavaScript로 접근 가능해서 XSS 공격에 취약함
- httpOnly 쿠키는 JS가 아예 읽을 수 없어서, 설령 XSS가 뚫려도 토큰을 훔쳐가지 못함
- `path: '/api/auth'`로 쿠키 범위를 제한해서, 이 토큰이 불필요하게 다른 API 요청에까지 실려가지 않도록 최소화

### Refresh Token을 DB에 원문 대신 해시로 저장한 이유
- 만약 DB가 유출되더라도, 저장된 게 SHA-256 해시값이라 실제 토큰을 역산할 수 없음 (비밀번호를 bcrypt로 해싱하는 것과 같은 원리)

### 로테이션(Rotation)이 왜 필요한가
- Refresh Token을 한 번 쓰면 즉시 폐기(revoked)하고 새 토큰을 발급
- 만약 공격자가 탈취한 Refresh Token을 쓰려고 시도했는데 이미 사용자가 그 토큰으로 갱신을 마친 상태라면, 공격자의 요청은 "이미 폐기된 토큰"으로 거부됨 → 재사용 공격(Replay Attack) 방어

### 자동 갱신은 어떻게 동작하는가
1. 프론트의 axios 요청 인터셉터가 모든 요청에 Access Token을 자동으로 붙임
2. 응답 인터셉터가 401(인증 실패)을 감지하면, 그 요청을 잠깐 보류하고 `/api/auth/refresh`를 먼저 호출
3. 새 Access Token을 받으면 실패했던 원래 요청을 새 토큰으로 재시도
4. 여러 요청이 동시에 401을 받아도 refresh 요청은 한 번만 나가도록 `isRefreshing` 플래그와 대기열(`refreshSubscribers`)로 제어 — 이걸 안 하면 토큰 갱신 요청이 동시에 여러 번 나가서 로테이션 로직과 충돌할 수 있음

---

## 4. 데이터 모델 설계 — User와 Employee를 분리한 이유

가장 많이 받을 법한 질문입니다.

- **User**: 로그인 계정 (이메일, 비밀번호 해시, 역할)
- **Employee**: 실제 인사정보 (부서, 직급, 입사일)

이렇게 나눈 이유는 **관리자 계정이 반드시 "직원"일 필요는 없기 때문**입니다. 예를 들어 시스템 관리 목적으로만 만든 `admin@erp.com` 계정은 User는 있지만 Employee 레코드는 없습니다. 실제로 이 설계 때문에, admin 계정으로 로그인해서 "출근 체크"를 누르면 "직원 정보가 없는 계정입니다"라는 에러가 발생하도록 처리했는데, 이건 버그가 아니라 의도된 도메인 규칙입니다.

### 관계 요약
- `User 1 : 1 Employee` (User가 없으면 Employee도 없음, Employee 생성 시 User를 트랜잭션으로 함께 생성)
- `Employee 1 : N Salary` (직원 한 명이 여러 달의 급여 기록을 가짐)
- `Employee 1 : N Attendance` (직원 한 명이 여러 날의 출퇴근 기록을 가짐, 하루 1건 Unique 제약)
- `Employee 1 : N Leave` (직원 한 명이 여러 건의 휴가 신청을 가짐)
- `Department 1 : N Employee`
- `Item 1 : N InventoryTransaction` (품목 하나에 입고/출고 이력이 여러 건)
- `AccountingEntry`, `Department`는 다른 도메인과 느슨하게 결합 (회계는 특정 직원에 종속되지 않는 독립 도메인으로 설계)

---

## 5. 도메인별 핵심 비즈니스 로직

### 5.1 인사관리 — 트랜잭션으로 계정과 인사정보를 함께 생성
직원 등록 시 `User`와 `Employee`를 **하나의 Prisma `$transaction`** 안에서 생성합니다. 만약 User는 만들어졌는데 Employee 생성이 실패하면, 로그인은 되지만 인사정보가 없는 유령 계정이 생기는 데이터 불일치가 발생하기 때문입니다. 트랜잭션으로 묶으면 둘 중 하나라도 실패하면 전부 롤백됩니다.

### 5.2 부서관리 — 참조 무결성을 애플리케이션 레벨에서 한 번 더 검증
부서 삭제 시, DB 외래키 제약만으로도 삭제가 막히긴 하지만, 사용자에게 "이 부서에 아직 직원이 N명 있습니다"라는 **명확한 에러 메시지**를 보여주기 위해 삭제 전에 `_count.employees`를 먼저 조회해서 검증합니다.

### 5.3 급여관리 — 순지급액을 백엔드에서 계산하는 이유
`netPay = baseSalary + allowance - deduction` 계산을 프론트가 아니라 **백엔드에서** 최종 확정합니다. 프론트의 실시간 미리보기는 사용자 경험을 위한 것일 뿐, 실제 저장되는 값은 서버가 재계산한 값입니다. 클라이언트가 보낸 계산 결과를 그대로 믿으면, 악의적인 사용자가 요청을 조작해 순지급액을 부풀릴 수 있기 때문입니다. 같은 직원의 같은 달 급여는 `@@unique([employeeId, payMonth])` 제약으로 DB 레벨에서 중복을 막습니다.

### 5.4 재고관리 — 입출고와 재고 수량을 원자적으로 처리
입출고 트랜잭션 기록(`InventoryTransaction` 생성)과 품목의 현재 재고 수량 업데이트(`Item.quantity` 증감)를 **하나의 트랜잭션**으로 묶었습니다. 둘 중 하나만 성공하면 "입고 기록은 있는데 재고는 안 늘어난" 불일치가 생기기 때문입니다. 출고 시에는 트랜잭션 시작 전에 현재 재고보다 많은 수량을 출고하려는지 미리 검증합니다.

### 5.5 회계관리 — 카테고리를 콤보박스+직접입력 혼합으로 설계한 이유
처음엔 고정된 콤보박스였는데, 실무에서는 미리 정의 못한 카테고리가 계속 생기기 마련이라 Ant Design의 `mode="tags"`로 "선택도 되고 새로 입력도 되는" 방식으로 바꿨습니다. 이 과정에서 `mode="tags"` Select가 **항상 배열**을 반환한다는 걸 발견해서, 제출 직전에 배열을 문자열로 변환하는 `toSingleString()` 헬퍼를 만들어 백엔드 DTO(문자열 타입)와 맞췄습니다.

### 5.6 근태관리 — 출근 체크의 멱등성(Idempotency) 처리
`Attendance`는 `@@unique([employeeId, workDate])` 제약이 있어서, 하루에 한 직원당 레코드가 하나만 존재합니다. 출근 체크는 Prisma의 `upsert`(있으면 갱신, 없으면 생성)로 처리해서, 오늘 처음 출근 체크하면 새로 만들고, 이미 있는데 다시 누르면 "이미 체크했습니다" 에러를 반환합니다.

### 5.7 휴가 승인 — 상태 기계(State Machine)
휴가 신청은 `PENDING → APPROVED` 또는 `PENDING → REJECTED`로만 전이 가능하고, 이미 처리된 건은 다시 승인/반려할 수 없도록 서비스 로직에서 상태를 검증합니다. 취소도 `PENDING` 상태에서만 가능하고, 본인 신청만 취소 가능하도록 권한 검증이 들어갑니다 (관리자는 예외).

### 5.8 대시보드 — 여러 도메인 데이터를 병렬로 집계
`Promise.all()`로 8개의 서로 다른 쿼리(직원 수, 부서 수, 재고 부족 품목 수 등)를 동시에 실행해서 응답 시간을 단축했습니다. 순차 실행했다면 쿼리 개수만큼 왕복 시간이 누적됐을 겁니다.

---

## 6. 프론트엔드 아키텍처

### Feature 기반 폴더 구조를 쓴 이유
`components/`, `pages/`, `api/`처�럼 **타입별**로 나누는 대신, `features/employees/`, `features/salaries/`처럼 **도메인별**로 묶었습니다. 이렇게 하면 "급여 기능을 수정해야 한다"고 할 때 여러 폴더를 오갈 필요 없이 `features/salaries/` 하나만 보면 됩니다. 프로젝트가 커질수록 이 방식이 유지보수에 유리합니다.

### React Query(TanStack Query)를 쓴 이유
서버에서 가져온 데이터(직원 목록, 급여 내역 등)는 "서버 상태"이지 "클라이언트 상태"가 아닙니다. React Query는 이런 서버 상태에 특화되어, 캐싱·자동 재요청·로딩/에러 상태 관리를 직접 구현하지 않아도 되게 해줍니다. 예를 들어 직원을 등록하면 `queryClient.invalidateQueries(['employees'])` 한 줄로 목록이 자동 갱신됩니다.

### Zustand를 쓴 이유 (Redux 대신)
인증 상태(토큰, 사용자 정보)처럼 전역이지만 단순한 상태는 Redux의 보일러플레이트(액션, 리듀서, 디스패치)가 과할 수 있습니다. Zustand는 훅 하나로 전역 상태를 만들 수 있고, `persist` 미들웨어로 localStorage 저장까지 간단히 처리했습니다.

### Ant Design의 `message` API를 정적 호출에서 훅 방식으로 바꾼 이유
Ant Design 최신 버전은 정적 `message.error()` 호출이 React Context 기반 동적 테마와 맞지 않아 **화면에 아무것도 안 뜨는** 문제가 있었습니다. `<App>` 컴포넌트로 트리를 감싸고 `App.useApp()` 훅으로 `message`를 받아 쓰는 방식으로 바꿔 해결했습니다. 이건 최신 라이브러리 버전의 브레이킹 체인지를 실제로 디버깅하며 배운 부분입니다.

---

## 7. 실제로 겪은 문제와 해결 과정 (상세)

### 문제 1: Prisma 7 스키마 검증 실패
- **증상**: `npx prisma generate` 실행 시 `The datasource property 'url' is no longer supported in schema files` 에러
- **원인**: Prisma 7부터 연결 문자열을 스키마 파일이 아니라 별도의 `prisma.config.ts`에서 관리하도록 아키텍처가 바뀜
- **해결**: `schema.prisma`에서 `url` 라인 제거, `prisma.config.ts` 신설, `@prisma/adapter-pg` 드라이버 어댑터를 `PrismaClient` 생성자에 주입하는 방식으로 전환

### 문제 2: 급여 등록 API가 500 에러를 내는데 원인을 알 수 없었음
- **증상**: 프론트에서 500 에러만 보이고, 백엔드 콘솔에는 아무 로그도 없었음
- **원인 추적**: 전역 예외 필터(`HttpExceptionFilter`)가 예외를 잡아서 응답은 보내지만, 콘솔에 로그를 남기는 코드가 없었음. `Logger`를 필터에 추가해 가시성을 확보한 뒤, 실제 원인이 `PrismaService`가 초기화되는 시점에 `.env`가 아직 로드되지 않아 `DATABASE_URL`이 `undefined`였던 것으로 밝혀짐
- **해결**: `main.ts` 최상단에 `import 'dotenv/config'`를 추가해 다른 모든 import보다 먼저 환경변수가 로드되도록 순서를 고정

### 문제 3: React가 "Objects are not valid as a React child" 에러로 죽음
- **원인**: 백엔드 예외 필터가 NestJS의 기본 예외 객체(`{message, error, statusCode}`)를 한 번 더 감싸서 이중 래핑된 상태로 응답했고, 프론트는 `message`가 문자열일 거라 가정하고 그대로 렌더링하려다 객체를 렌더링하게 됨
- **해결**: 백엔드 필터에서 `HttpException.getResponse()`의 구조를 검사해 항상 문자열/배열만 `message` 필드로 내려주도록 정규화. 프론트에도 방어적으로 배열/객체/문자열을 모두 처리하는 `extractErrorMessage()` 헬퍼를 추가해 이중 방어

### 문제 4: 배포 시 Nginx 500 에러
- **증상**: 빌드도 잘 되고 설정도 맞는데 브라우저에서 500
- **원인**: `/var/log/nginx/error.log`에서 `Permission denied` 확인. 리눅스는 홈 디렉토리 기본 권한이 `750`이라, Nginx를 실행하는 `www-data` 사용자가 `/home/사용자명/` 자체에 진입할 권한이 없었음 (하위 폴더 권한이 다 열려 있어도 상위 폴더가 막혀 있으면 소용없음)
- **해결**: 홈 디렉토리 전체를 공개하는 대신 `chmod o+x ~`로 "통과 권한(실행 권한)"만 부여. 읽기 권한(r)은 주지 않아 `ls`로 목록은 볼 수 없고, 정확한 경로를 아는 파일에만 접근 가능하도록 최소 권한 원칙 적용

### 문제 5: PM2와 systemd가 충돌해서 서비스가 안 뜸
- **원인**: 수동으로 `pm2 start`한 데몬이 이미 떠 있는 상태에서 `systemctl start`로 또 시작하려 하니 `Type=forking` 방식의 기대와 실제 동작이 어긋나며 반복 실패
- **해결**: `pm2 kill`로 기존 데몬을 완전히 종료한 뒤, systemd가 관리하는 프로세스로 새로 시작. 이후 `pm2 save`로 목록을 저장해 재부팅 시에도 `pm2 resurrect`가 같은 프로세스를 복원하도록 구성

---

## 8. 인프라 심화 — Docker, CI/CD, 부하테스트

배포를 PM2 단일 서버 방식에서 한 단계 더 발전시켜, 컨테이너 오케스트레이션과 자동화된 배포 파이프라인, 실측 성능 검증까지 구축했습니다.

### 왜 Docker Compose로 전환했는가

기존 PM2 방식은 백엔드 프로세스 하나만 관리했고, PostgreSQL/Redis는 호스트에 직접 설치되어 있었습니다. 이 구조는 "로컬에서만 도는 설정"에 의존하게 되어, 다른 환경으로 옮기거나 팀원과 공유하기 어렵습니다. Docker Compose로 전환하면서 PostgreSQL, Redis, 백엔드, 프론트엔드(Nginx) 4개 서비스를 각각 독립된 컨테이너로 분리하고, `docker-compose.yml` 하나로 전체 스택을 재현 가능하게 만들었습니다.

### 왜 GitHub Actions에 Self-hosted Runner를 썼는가

배포 대상 서버가 클라우드 VM이 아니라 개인 PC(WSL2)였기 때문에, GitHub의 클라우드 Runner가 SSH로 접속하려면 공인 IP와 포트 개방이 필요해 보안 리스크가 컸습니다. 대신 **Self-hosted Runner**를 배포 서버에 직접 설치해서, 이 서버가 GitHub에 먼저 연결을 여는 방식(아웃바운드 전용)으로 전환했습니다. 포트를 열지 않고도 CI/CD를 완성할 수 있었고, 이는 사내망·폐쇄망 환경에서 실제로 쓰이는 배포 패턴이기도 합니다.

### 헬스체크와 자동 롤백은 어떻게 구현했는가

1. 배포 전, 현재 운영 중인 Docker 이미지를 `:previous` 태그로 백업
2. 새 이미지 빌드 및 배포
3. `/api/health`(인증 없이 접근 가능한 엔드포인트, DB 연결까지 확인)를 3초 간격으로 최대 10회 재시도
4. 통과하면 배포 완료, **실패하면 `:previous` 태그를 `:latest`로 되돌리고 컨테이너를 재생성**

이 구조의 한계도 인지하고 있습니다: 애플리케이션 코드(이미지)는 롤백되지만, 이미 적용된 DB 마이그레이션은 자동으로 되돌아가지 않습니다. 실무에서는 이를 "Forward-only Migration" 전략으로 대응하는데, 문제가 생기면 되돌리는 마이그레이션이 아니라 앞으로 나아가는 새 마이그레이션을 작성하는 방식입니다.

### k6로 무엇을, 왜 검증했는가

배포 자체가 잘 되는 것과, 실제 트래픽 아래에서 성능이 나오는 것은 다른 문제입니다. 로그인 → 대시보드 조회 → 직원 목록 조회로 이어지는 실사용 흐름을 시나리오로 만들어, 0명 → 30명까지 점진적으로 부하를 올리며 응답시간과 실패율을 측정했습니다. 결과는 p95 53.18ms, 실패율 0%로, 현재 단일 인스턴스 구조에서 30명 규모 동시 사용자를 무리 없이 처리함을 실측으로 확인했습니다.

### 이 과정에서 겪은 트러블슈팅

- **GitHub Actions Runner 설치 폴더가 실수로 통째로 커밋됨**: `git add .`가 `actions-runner/` 디렉토리(Node.js 바이너리 포함, 수백MB)까지 스테이징해서 GitHub의 100MB 파일 크기 제한에 걸려 push가 거부됨. `git reset --soft`로 커밋만 취소하고, `.gitignore`에 `actions-runner/`를 추가해 재발 방지.
- **Docker Compose 프로젝트 이름 불일치로 컨테이너 충돌**: 로컬에서 수동 실행할 때와 GitHub Actions Runner가 실행할 때 작업 디렉토리 경로가 달라, Compose가 서로 다른 프로젝트로 인식하면서 `container_name`이 충돌. `docker-compose.yml` 최상단에 `name: erp-system`을 명시해 프로젝트 이름을 고정하여 해결.
- **Nginx Alpine 이미지에 `wget`이 없어 헬스체크 실패**: 컨테이너는 정상 실행 중인데 healthcheck만 계속 `unhealthy`로 표시됨. `wget` 대신 nginx 자체 내장 명령인 `nginx -t`(설정 유효성 검사)로 헬스체크 방식을 변경해 별도 패키지 설치 없이 해결.
- **k6 부하테스트에서 로그인 실패율 100%**: 코드 버그로 의심했으나, 원인은 Docker Compose로 새로 만든 PostgreSQL 컨테이너에 시드 데이터(관리자 계정)가 없었던 것. 실제 애플리케이션 문제가 아니라 테스트 환경 준비 미흡이었음을 진단하고, 이후 부하테스트 전 데이터 상태를 먼저 점검하는 절차를 습관화.

## 9. 예상 질문 Q&A

**Q. 왜 Access Token을 15분으로 짧게 잡았나요? 사용자가 불편하지 않나요?**
A. Refresh Token 자동 갱신 덕분에 사용자는 만료를 전혀 느끼지 못합니다. 실제로 만료 시간을 10초로 줄여서 테스트했을 때도 화면이 끊기지 않고 정상 작동하는 걸 확인했습니다. 짧게 잡는 이유는, 토큰이 탈취되더라도 공격자가 쓸 수 있는 시간을 최소화하기 위해서입니다.

**Q. 재고 출고 시 동시성 문제는 없나요? (두 사람이 동시에 마지막 재고를 출고하려 하면?)**
A. 현재는 Prisma 트랜잭션 안에서 재고를 조회하고 검증한 뒤 감소시키는데, 극단적인 동시 요청 상황에서는 레이스 컨디션 여지가 있습니다. 개선한다면 `SELECT ... FOR UPDATE` 같은 비관적 락이나, `quantity` 컬럼에 조건부 업데이트(`WHERE quantity >= ?`)를 거는 방식으로 보완할 수 있습니다. *(이 답변은 스스로 개선점을 인지하고 있다는 걸 보여주는 좋은 포인트입니다.)*

**Q. 왜 급여 계산을 프론트에서 안 하고 백엔드에서 하나요?**
A. 클라이언트가 보낸 값은 신뢰할 수 없기 때문입니다. 프론트의 실시간 계산은 사용자 경험(입력하면서 바로 결과 보기)을 위한 것이고, 실제 저장값은 서버가 같은 공식으로 재계산해서 검증 후 저장합니다.

**Q. 이 프로젝트에서 가장 어려웠던 부분은?**
A. Prisma 7의 아키텍처 변경 대응이었습니다. 공식 문서가 아직 충분히 축적되지 않은 최신 버전이라, 에러 메시지 하나를 보고 마이그레이션 가이드 문서를 직접 찾아가며 `prisma.config.ts`와 Driver Adapter 패턴을 새로 학습해야 했습니다.

**Q. 테스트 코드는 작성했나요?**
A. 아직 작성하지 못했고, 이후 개선 과제로 단위 테스트와 E2E 테스트 도입을 계획하고 있습니다. *(솔직하게 답하고 개선 계획을 함께 제시하는 게 좋습니다.)*

**Q. 왜 마이크로서비스가 아니라 모놀리식으로 만들었나요?**
A. 현재 규모(1인 개발, 6개 도메인)에서는 모놀리식이 배포와 디버깅이 훨씬 단순합니다. NestJS의 모듈 구조 자체가 도메인별로 이미 잘 분리되어 있어서, 나중에 트래픽이나 팀 규모가 커지면 특정 모듈(예: 재고관리)만 별도 서비스로 분리하는 점진적 전환이 어렵지 않은 구조로 설계했습니다.

**Q. 왜 GraphQL이 아니라 REST를 썼나요?**
A. ERP처럼 도메인 경계가 명확하고 화면별로 필요한 데이터 형태가 어느 정도 고정된 경우, REST가 구조를 이해하고 Swagger로 문서화하기에 더 직관적이라고 판단했습니다.

### 인프라 관련 추가 질문

**Q. 왜 클라우드 Runner 대신 Self-hosted Runner를 썼나요? 더 어려운 방법 아닌가요?**
A. 배포 대상이 클라우드 VM이 아니라 개인 서버였기 때문에, 클라우드 Runner가 SSH로 접속하려면 공인 IP 노출과 포트 개방이 필요해 보안상 부담이 컸습니다. Self-hosted Runner는 서버가 GitHub에 먼저 연결을 여는 아웃바운드 방식이라 포트를 열 필요가 없고, 이는 실제로 사내망 배포에서 흔히 쓰이는 패턴입니다. 설정은 조금 더 필요하지만, 보안 모델 자체가 더 안전합니다.

**Q. 자동 롤백이 완벽한가요? 한계는 없나요?**
A. 애플리케이션 이미지는 자동으로 이전 버전으로 되돌아가지만, 이미 적용된 DB 스키마 변경(마이그레이션)은 자동 롤백되지 않습니다. 이건 의도적인 설계 한계이자 업계 일반적인 관행이기도 합니다 — 실무에서는 "실패하면 되돌리기"보다 "실패하면 고친 새 마이그레이션을 앞으로 배포하기"(Forward-only) 전략을 더 선호하는데, 데이터가 이미 변경된 상태에서 스키마만 되돌리면 오히려 불일치가 생길 수 있기 때문입니다.

**Q. k6 테스트에서 30명이 최선인가요? 왜 더 큰 규모로 안 했나요?**
A. 30명은 현재 인프라(단일 백엔드 인스턴스, 단일 PostgreSQL 컨테이너, 로컬 환경)에서 의미 있게 측정 가능한 범위로 설정한 것이지, 최대치를 검증한 건 아닙니다. p95가 53ms로 기준(800ms) 대비 15배 여유가 있어서, 더 높은 부하(100명, 500명)로 확장했을 때 어디서 병목이 생기는지(DB 커넥션 풀, CPU, 메모리)를 확인하는 게 다음 단계라고 생각합니다. 이 부분은 향후 개선 계획에 명시해뒀습니다.

## 10. 스스로 짚어볼 만한 개선 포인트 (면접에서 먼저 언급하면 좋은 것들)

- 역할별(ADMIN/MANAGER/EMPLOYEE) 프론트엔드 라우팅 접근 제어는 아직 백엔드 API 레벨에서만 강제되고, 프론트 화면 단에서는 미흡함
- 테스트 코드(단위/E2E) 부재
- HTTPS 미적용 (현재는 HTTP로 로컬/사내망 배포 가정)
- 재고 출고 동시성 제어가 낙관적 수준에 머물러 있음
- 부하테스트가 조회(Read) 위주이며, 쓰기(Write) 작업이 포함된 동시성 시나리오는 아직 검증하지 않음
- DB 마이그레이션 실패 시 자동 롤백이 되지 않는 한계 (Forward-only 전략으로 대응 예정)
