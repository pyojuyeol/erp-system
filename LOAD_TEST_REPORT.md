# 부하 테스트 결과 리포트 (k6)

## 테스트 목적

Docker Compose로 배포된 프로덕션급 환경(Nginx 리버스 프록시 + NestJS 백엔드 + PostgreSQL + Redis)이 동시 사용자 트래픽 아래에서도 안정적으로 응답하는지 검증합니다.

## 테스트 환경

| 항목 | 내용 |
|---|---|
| 대상 | `http://localhost:8090` (Nginx → 백엔드 리버스 프록시, Docker Compose 기동) |
| 도구 | k6 v2.2.0 |
| 실행 위치 | 애플리케이션과 동일한 호스트 (네트워크 지연 없는 로컬 벤치마크) |
| 테스트 시나리오 | 실사용 흐름: 로그인 → 대시보드 조회 → 직원 목록 조회 |

## 시나리오 1 — 기본 헬스체크 부하 (사전 점검)

인증 없이 접근 가능한 `/api/health` 엔드포인트에 20명의 가상 사용자(VU)가 30초간 지속 요청.

| 지표 | 결과 | 기준(threshold) | 통과 여부 |
|---|---|---|---|
| p95 응답시간 | 9.38ms | < 200ms | ✅ |
| 실패율 | 0.00% | < 1% | ✅ |
| 총 요청 수 | 2,940건 | - | - |
| 처리량 | 97.4 req/s | - | - |

인증이나 DB 조회가 없는 가장 가벼운 엔드포인트라 응답이 매우 빠르게 나왔습니다. 인프라(Nginx, 컨테이너 네트워킹)가 병목 없이 정상 동작함을 확인했습니다.

## 시나리오 2 — 실사용 흐름 부하 테스트

**부하 프로파일**: 0명 → 10명(20초) → 10명 유지(40초) → 30명으로 급증(20초) → 30명 유지(30초) → 0명으로 감소(15초), 총 2분 5초

각 가상 사용자는 반복마다 다음 흐름을 수행합니다.
1. `POST /api/auth/login` — 로그인, JWT 발급
2. `GET /api/dashboard/summary` — 대시보드 통계 (직원 수, 재고, 회계 등 여러 테이블 집계 쿼리 포함)
3. `GET /api/employees` — 직원 목록 조회 (JOIN 쿼리 포함)

### 결과 요약

| 지표 | 결과 | 기준(threshold) | 통과 여부 |
|---|---|---|---|
| p95 응답시간 (전체) | 53.18ms | < 800ms | ✅ |
| 요청 실패율 | 0.00% | < 1% | ✅ |
| 로그인 실패율 | 0.00% | < 1% | ✅ |
| 총 반복 횟수 | 993회 | - | - |
| 총 HTTP 요청 수 | 2,979건 | - | - |
| 최대 동시 사용자(VU) | 30명 | - | - |

### 엔드포인트별 상세

| 엔드포인트 | 평균 응답시간 | p95 응답시간 |
|---|---|---|
| 대시보드 (`/dashboard/summary`) | 6.77ms | 10.47ms |
| 전체 평균 (로그인+대시보드+직원목록) | 21.36ms | 53.18ms |

대시보드는 8개의 서로 다른 집계 쿼리를 `Promise.all()`로 병렬 실행하도록 설계했는데(직원 수, 부서 수, 재고 부족 품목, 이번 달 급여/회계 등), 이 최적화 덕분에 여러 테이블을 조회함에도 평균 6.77ms의 빠른 응답을 유지했습니다.

## 관찰 사항

**최대 30명 동시 사용자 부하에서도 전체 요청의 95%가 53ms 이내에 처리**되어, 설정한 기준(800ms)에 비해 약 15배 여유가 있는 것으로 확인되었습니다. 현재 구조(단일 백엔드 인스턴스, PostgreSQL 단일 컨테이너)에서 30명 규모의 동시 사용자는 무리 없이 처리 가능한 수준입니다.

## 테스트 중 발견 및 해결한 문제

최초 시나리오 테스트 실행 시 로그인 실패율이 100%로 나온 적이 있었습니다. 원인은 코드 결함이 아니라 **테스트 대상 환경(Docker Compose로 새로 생성한 PostgreSQL)에 시드 데이터가 없었던 것**으로 확인되어, `prisma/seed.ts`를 재실행해 해결했습니다. 이 경험을 통해 부하 테스트 이전에 테스트 환경의 데이터 상태를 반드시 사전 점검해야 한다는 점을 확인했습니다.

## 향후 확장 계획

- **더 높은 동시 사용자 규모**(100명, 500명)로 확장하여 병목 지점(DB 커넥션 풀, PM2 클러스터 모드 필요 여부) 파악
- **급여/재고 등 쓰기(Write) 작업이 포함된 시나리오** 추가 (현재는 조회 위주)
- **k6 결과를 CI/CD 파이프라인에 통합**하여, 배포 시마다 성능 회귀(regression)를 자동 감지
- **Grafana + InfluxDB 연동**으로 시계열 대시보드 구축, 부하 증가 구간에서의 응답시간 변화를 시각적으로 추적
